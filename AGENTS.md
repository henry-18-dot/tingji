# 项目入口

本目录同时保留本地听记与云端网站，先确认用户要改的是哪一版。

- 用户提到「网站」「云端版」「给朋友使用」或 `tingji-henry.vercel.app` 时，先读 [tingji-web/AI_HANDOFF.md](tingji-web/AI_HANDOFF.md)，在 `tingji-web/` 内开展工作。
- `tingji/`、根目录 `server.py`、`web/` 和本地启动器属于原有本地项目，保留已有修改与数据。
- `tingji-cloud/` 是早期骨架，不是当前生产网站。
- 交接文档中的线上状态是带日期的记录，接手时按任务需要刷新；不要重复注册、购买资源或盲目重交付费任务。
- 凭据通过受限运行配置使用，不写入代码、文档、日志或发布包。
